<h1>Listing queries</h1>
<% @queries.each do |query| %>
<hr>
  <h2><%= query.q %></h2>
  <h4><%= query.created_at %></h4>
  <% query.pages.each do |page| %>
    <%= "#{page.title}: " %> <%= link_to page.url, page.url %> <br>
  <% end %>
<% end %>
<%= paginate @queries %>
